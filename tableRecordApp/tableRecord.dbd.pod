=title Table Record (table)

The C<table> record holds a column-oriented table: a set of named columns, each
an array of up to C<MAXROWS> elements of a per-column data type. It is intended
to be served over PV Access as a pvData C<NTTable>; the bundled C<<< QSRV2 >>>
(pvxs) integration publishes the record's columns, labels and timestamp as the
structured fields of an C<NTTable>.

Rather than a single value field, the record exposes each column as a I<bundle>
of fixed-name fields, in the same spirit as the aSub record's A..U inputs. A
column C<I<nn>> (hex C<00>..C<3F>) is described by C<CI<nn>NAME>, C<CI<nn>LABEL>,
C<CI<nn>TYPE> and C<CI<nn>INP>, with its data held in C<CI<nn>VAL> and its
runtime status in C<CI<nn>CHGD> and C<CI<nn>NROWS>. Up to 64 such I<data>
columns may be configured.

A second family of up to 64 I<optional> columns (C<COI<nn>...>) is provided for
per-column metadata: each optional column carries exactly one value per data
column (so it is sized to C<NUMCOLS> rows, not C<MAXROWS>) and is published
alongside the table, typically as a column attribute.

Columns are configured statically in the database (or filled in by device
support) and validated once, when the record initializes; thereafter the column
layout is fixed and only the column I<data> changes as the record processes.

=recordtype table

=cut

recordtype(table) {

=head2 Parameter Fields

The record-specific fields are described below, grouped by function. The 64 data
columns share an identical set of per-column fields, named with the column index
C<00>..C<3F> embedded in the field name (for example C<C00NAME>, C<C01NAME>, ...,
C<C3FNAME>); only the column C<00> fields are tabulated here, the rest follow the
same pattern. The 64 optional columns are named C<CO00...>..C<CO3F...> in the
same way.

=head3 Scan Parameters

The table record has the standard fields for specifying under what circumstances
it will be processed. These fields are listed in
L<Scan Fields|dbCommonRecord/Scan Fields>.

=head3 Table Configuration

C<INP> is an optional input link for the record as a whole. Device support may
use it (for example, the simulation and CSV device supports read their table
description or contents through C<INP>); the C<<< Soft Channel >>> support
ignores it and instead reads each column's own C<CI<nn>INP> link.

C<MAXROWS> is the row capacity allocated for every data column. It must be set
before initialization (it is read-only afterwards); if left at zero,
C<init_record> forces it to 1. Each data column's value buffer is allocated to
hold C<MAXROWS> elements.

C<NUMCOLS> and C<NUMOPTCOLS> report how many data and optional columns are
active. They are B<not> set by the user: C<init_record> computes them by counting
the configured (non-empty) column names, and they are read-only
(C<<< SPC_NOMOD >>>).

=fields INP, MAXROWS, NUMCOLS, NUMOPTCOLS

=head3 Data Columns (C00 .. C3F)

Each data column is configured by four fields and exposes three more at runtime:

=over

=item *

C<CI<nn>NAME> - the column name. A column is "present" if its name is non-empty.
Names must match C<< [A-Za-z_][A-Za-z0-9_]* >>, must be unique across data
columns, and must be contiguous (no gaps): column C<I<nn>> may only be named if
all lower-numbered columns are also named. These rules are checked by
C<init_record>, which rejects the record (logging an error) if any is violated.
The name is fixed after initialization (C<<< SPC_NOMOD >>>) and becomes the
field label of the corresponding C<NTTable> column.

=item *

C<CI<nn>LABEL> - an optional human-readable column label carried alongside the
name. If empty, consumers typically fall back to the name.

=item *

C<CI<nn>TYPE> - the column's element type, chosen from the C<menuFtype> menu
(C<STRING>, C<CHAR>, C<SHORT>, C<LONG>, C<DOUBLE>, ...). Defaults to C<DOUBLE>.
It is read-only after initialization, since the value buffer is allocated for
this type. C<STRING> columns use a B<variable-length string encoding>
(see L</Variable Strings>) so cells may hold strings longer than the usual
39-character limit; PV Access clients receive the full string, while Channel
Access clients see a truncated preview for strings exceeding 39 characters.

=item *

C<CI<nn>INP> - an optional per-column input link. The C<<< Soft Channel >>>
device support reads the column's data from this link (a constant array, a
database link, or a channel-access link).

=item *

C<CI<nn>VAL> - the column data buffer, an array of C<MAXROWS> elements of type
C<CI<nn>TYPE>. It is process-passive (C<pp(TRUE)>): a C<dbPutField> to it triggers
processing. Reading or writing it through Channel Access / dbAccess uses the
column's element type and reports C<CI<nn>NROWS> elements as the current length.

=item *

C<CI<nn>NROWS> - the number of valid rows currently in the column (0..MAXROWS),
maintained by device support / array puts. Read-only.

=item *

C<CI<nn>CHGD> - a "changed this cycle" flag. It is cleared at the start of
C<process> and set when the column's data is updated, so a publisher can send
only the columns that actually changed.

=back

=fields C00NAME, C00LABEL, C00TYPE, C00INP, C00VAL, C00NROWS, C00CHGD

=head3 Optional Columns (CO00 .. CO3F)

Optional columns hold one value per data column rather than C<MAXROWS> rows; an
optional column's buffer is therefore sized to C<NUMCOLS> elements. They are
intended for per-column metadata (for example a units or description attribute
for each data column) that is published together with the table.

They follow the same bundle pattern and the same validation rules as data
columns, with one addition: an optional column name may contain a single dotted
component, C<< prefix.field >> (matching
C<< [A-Za-z_][A-Za-z0-9_]*(\.[A-Za-z_][A-Za-z0-9_]*)? >>), which lets a consumer
map the optional column onto a nested attribute. C<COI<nn>NAME> and
C<COI<nn>TYPE> are fixed after initialization; C<COI<nn>VAL>, C<COI<nn>NROWS> and
C<COI<nn>CHGD> behave as for data columns.

=fields CO00NAME, CO00TYPE, CO00INP, CO00VAL, CO00NROWS, CO00CHGD

=head2 Record Support

=head3 Record Support Routines

=head4 init_record

Called twice (pass 0 then pass 1). On pass 0 it:

=over

=item 1.

Verifies that device support is attached: the DSET must be present, and must
declare at least 5 routines with a non-NULL C<read_table>. If not, an error is
logged and initialization fails.

=item 2.

Forces C<MAXROWS> to 1 if it was left at 0.

=item 3.

Calls the device support's C<init_record> (if any) so it can allocate memory and
fill in column names/types. If that routine returns C<<< TABLEREC_DEVINIT_PASS1 >>>
the record arranges to call it again in pass 1.

=item 4.

Validates the data column names (format, uniqueness, no gaps as described above)
and sets C<NUMCOLS> to the number found. The optional column names are validated
the same way and C<NUMOPTCOLS> is set.

=item 5.

Allocates any column value buffers not already provided by device support:
C<MAXROWS> elements for each data column, C<NUMCOLS> elements for each optional
column.

=back

On pass 1, if device support requested it, the device C<init_record> is called
once more (now able to read links and other records).

=head4 process

See L</Record Processing> below.

=head4 special

C<CI<nn>VAL> and C<COI<nn>VAL> accept array puts via C<special>. Before the data
lands (C<after=0>), a C<STRING> column's previous variable-length cells are
released; after the put (C<after=1>), the column's C<CHGD> flag is set. Puts to
inactive columns are rejected. The column C<NAME> and C<TYPE> fields are
read-only at run time (C<<< SPC_NOMOD >>>) and cannot be changed once the record
has initialized.

=head4 cvt_dbaddr, get_array_info, put_array_info

These present each C<CI<nn>VAL>/C<COI<nn>VAL> field as an array whose element type
is the column's C<TYPE>, whose capacity is C<MAXROWS> (data) or C<NUMCOLS>
(optional columns), and whose current length is the column's C<NROWS>.

=head3 Record Processing

Routine C<process> implements the following algorithm:

=over

=item 1.

If device support or its C<read_table> routine is missing, set C<PACT> TRUE, log
an error and return, so processing is not retried (avoiding error storms).

=item 2.

Clear the C<CHGD> flag of every active data and optional column.

=item 3.

Call the device support C<<< read_table >>> routine to refresh the column data.
If it succeeds, clear C<UDF>.

=item 4.

Update the time stamp and reset alarms.

=item 5.

Post monitor events for the column data.

=item 6.

If a publisher has registered a notify hook in C<RPVT> (as the C<<< QSRV2 >>>
NTTable source does), call it synchronously while the record lock is held and
this cycle's C<CHGD> flags are still valid.

=item 7.

Scan the forward link, set C<PACT> FALSE and return.

=back

=head2 Device Support

=head3 Fields Of Interest To Device Support

Device support obtains a fresh table whenever C<read_table> is called, and is
chiefly interested in the column buffers and counts.

=fields PACT, DPVT, UDF, NUMCOLS, NUMOPTCOLS, MAXROWS

=head3 Device Support Interface

A table device support entry table (C<tabledset>) extends the standard DSET with
one routine:

  typedef struct {
      dset common;                              /* standard 5-entry DSET   */
      long (*read_table)(struct tableRecord *prec);
  } tabledset;

C<read_table> must refresh the active column buffers (and their C<NROWS>) and
return 0 on success. Device support that needs to allocate buffers or populate
column names/types does so from C<<< common.init_record >>>. Because reading
links is only safe after all records exist, support that must read links at init
returns C<<< TABLEREC_DEVINIT_PASS1 >>> from its pass-0 C<init_record> to ask the
record to call it again in pass 1.

=head3 Device Support For Soft Records

The C<<< Soft Channel >>> support (C<devTableSoft>) implements a table whose
columns are driven by their individual C<CI<nn>INP> / C<COI<nn>INP> links. At
initialization it loads any constant links into the column buffers; on each
C<read_table> it re-reads the non-constant links. C<STRING> columns are staged
through a temporary buffer and re-encoded into variable-length cells so that
numeric-to-string conversions cannot leave stale bytes in a cell.

=head2 Variable Strings

EPICS strings are normally limited to C<MAX_STRING_SIZE> bytes (40), leaving
at most 39 usable characters. C<STRING>-typed columns use a B<variable-length
string encoding> (defined in C<tableVStr.h>) that lets device support store
strings of arbitrary length, including strings with embedded C<NUL> bytes.

Each 40-byte cell in a C<STRING> column buffer is stored in one of three
formats, discriminated by two sentinel bytes within the cell:

=over

=item *

B<Type 1 - very short string> (length E<le> 31, no embedded C<NUL>): the string
bytes are stored directly in the cell, NUL-terminated. Fully compatible with
an ordinary C<DBF_STRING>.

=item *

B<Type 2 - short string> (32 E<le> length E<le> 39, no embedded C<NUL>): the
string bytes are stored directly in the cell, NUL-terminated. Also compatible
with an ordinary C<DBF_STRING>.

=item *

B<Type 3 - long string> (length E<gt> 39, or embedded C<NUL>): up to 31 bytes of
a UTF-8-clean preview are stored in the cell inline; a pointer to a
heap-allocated C<tablerecVStr> block carrying the full string is stored in
bytes 32..39 of the cell.

=back

PV Access clients (C<NTTable>) always receive the full string regardless of
encoding type. Channel Access and dbAccess clients see the ordinary
C<DBF_STRING> cell: the complete content for Types 1 and 2, and only the
31-byte UTF-8 preview for Type 3.

=head3 Writing long strings from device support

Type 3 cells can only be produced by device support code. The C API in
C<tableVStr.h> provides:

  void tablerec_vstr_write(void *colbuf, epicsUInt32 row,
                           const char *bytes, epicsUInt32 len);

The C++ wrapper in C<tableRecordUtil.h> provides C<write_string_column> and
C<vstr_write_cell> as higher-level helpers. External writes via C<caput>,
C<dbpf>, or PV Access are accepted but the C<DBF_STRING> type caps them at 39
characters, so they produce at most a Type 2 cell.

Overflow heap blocks are freed automatically whenever a cell is overwritten or
cleared. The C<special()> handler also frees any overflow pointer before an
external put can overwrite a cell, preventing leaks during normal IOC operation.
All access to a C<STRING> column buffer must be done while holding the record
lock (C<dbScanLock> / C<process> / C<dbPutField>).

=cut

    include "dbCommon.dbd"

    %
    %/* Device Support Entry Table */
    %struct tableRecord;
    %typedef struct {
    %    dset common;
    %    long (*read_table)(struct tableRecord *prec);
    %} tabledset;
    %#define HAS_tabledset
	%#define TABLEREC_DEVINIT_PASS1 2
    %#define TABLEREC_MAX_DATA_COLS 64
    %#define TABLEREC_MAX_OPT_COLS 64
    %

    field(INP,DBF_INLINK) {
        prompt("Input Specification")
        promptgroup("40 - Input")
        interest(1)
    }
    field(NUMCOLS, DBF_ULONG) {
        prompt("Number of active columns (set by device support)")
        special(SPC_NOMOD)
        interest(1)
    }
    field(MAXROWS, DBF_ULONG) {
        prompt("Max number of rows per column")
        special(SPC_NOMOD)
        interest(1)
        initial("1")
    }
    field(RPVT, DBF_NOACCESS) {
        prompt("Record Private")
        special(SPC_NOMOD)
        extra("void *rpvt")
        interest(4)
    }

    # ----------------------------------------------------------------
    # Column names - C00NAME .. C3FNAME
    # ----------------------------------------------------------------

    field(C00NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 00 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C01NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 01 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C02NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 02 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C03NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 03 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C04NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 04 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C05NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 05 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C06NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 06 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C07NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 07 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C08NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 08 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C09NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 09 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C0ANAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 0A name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C0BNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 0B name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C0CNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 0C name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C0DNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 0D name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C0ENAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 0E name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C0FNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 0F name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C10NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 10 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C11NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 11 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C12NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 12 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C13NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 13 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C14NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 14 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C15NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 15 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C16NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 16 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C17NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 17 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C18NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 18 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C19NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 19 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C1ANAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 1A name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C1BNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 1B name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C1CNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 1C name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C1DNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 1D name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C1ENAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 1E name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C1FNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 1F name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C20NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 20 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C21NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 21 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C22NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 22 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C23NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 23 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C24NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 24 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C25NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 25 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C26NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 26 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C27NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 27 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C28NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 28 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C29NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 29 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C2ANAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 2A name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C2BNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 2B name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C2CNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 2C name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C2DNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 2D name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C2ENAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 2E name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C2FNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 2F name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C30NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 30 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C31NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 31 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C32NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 32 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C33NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 33 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C34NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 34 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C35NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 35 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C36NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 36 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C37NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 37 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C38NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 38 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C39NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 39 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C3ANAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 3A name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C3BNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 3B name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C3CNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 3C name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C3DNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 3D name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C3ENAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 3E name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C3FNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Column 3F name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    # ----------------------------------------------------------------
    # Column labels - C00LABEL .. C3FLABEL
    # ----------------------------------------------------------------

    field(C00LABEL, DBF_STRING) {
        prompt("Column 00 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C01LABEL, DBF_STRING) {
        prompt("Column 01 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C02LABEL, DBF_STRING) {
        prompt("Column 02 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C03LABEL, DBF_STRING) {
        prompt("Column 03 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C04LABEL, DBF_STRING) {
        prompt("Column 04 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C05LABEL, DBF_STRING) {
        prompt("Column 05 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C06LABEL, DBF_STRING) {
        prompt("Column 06 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C07LABEL, DBF_STRING) {
        prompt("Column 07 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C08LABEL, DBF_STRING) {
        prompt("Column 08 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C09LABEL, DBF_STRING) {
        prompt("Column 09 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C0ALABEL, DBF_STRING) {
        prompt("Column 0A label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C0BLABEL, DBF_STRING) {
        prompt("Column 0B label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C0CLABEL, DBF_STRING) {
        prompt("Column 0C label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C0DLABEL, DBF_STRING) {
        prompt("Column 0D label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C0ELABEL, DBF_STRING) {
        prompt("Column 0E label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C0FLABEL, DBF_STRING) {
        prompt("Column 0F label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C10LABEL, DBF_STRING) {
        prompt("Column 10 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C11LABEL, DBF_STRING) {
        prompt("Column 11 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C12LABEL, DBF_STRING) {
        prompt("Column 12 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C13LABEL, DBF_STRING) {
        prompt("Column 13 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C14LABEL, DBF_STRING) {
        prompt("Column 14 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C15LABEL, DBF_STRING) {
        prompt("Column 15 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C16LABEL, DBF_STRING) {
        prompt("Column 16 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C17LABEL, DBF_STRING) {
        prompt("Column 17 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C18LABEL, DBF_STRING) {
        prompt("Column 18 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C19LABEL, DBF_STRING) {
        prompt("Column 19 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C1ALABEL, DBF_STRING) {
        prompt("Column 1A label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C1BLABEL, DBF_STRING) {
        prompt("Column 1B label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C1CLABEL, DBF_STRING) {
        prompt("Column 1C label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C1DLABEL, DBF_STRING) {
        prompt("Column 1D label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C1ELABEL, DBF_STRING) {
        prompt("Column 1E label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C1FLABEL, DBF_STRING) {
        prompt("Column 1F label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C20LABEL, DBF_STRING) {
        prompt("Column 20 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C21LABEL, DBF_STRING) {
        prompt("Column 21 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C22LABEL, DBF_STRING) {
        prompt("Column 22 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C23LABEL, DBF_STRING) {
        prompt("Column 23 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C24LABEL, DBF_STRING) {
        prompt("Column 24 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C25LABEL, DBF_STRING) {
        prompt("Column 25 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C26LABEL, DBF_STRING) {
        prompt("Column 26 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C27LABEL, DBF_STRING) {
        prompt("Column 27 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C28LABEL, DBF_STRING) {
        prompt("Column 28 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C29LABEL, DBF_STRING) {
        prompt("Column 29 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C2ALABEL, DBF_STRING) {
        prompt("Column 2A label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C2BLABEL, DBF_STRING) {
        prompt("Column 2B label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C2CLABEL, DBF_STRING) {
        prompt("Column 2C label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C2DLABEL, DBF_STRING) {
        prompt("Column 2D label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C2ELABEL, DBF_STRING) {
        prompt("Column 2E label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C2FLABEL, DBF_STRING) {
        prompt("Column 2F label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C30LABEL, DBF_STRING) {
        prompt("Column 30 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C31LABEL, DBF_STRING) {
        prompt("Column 31 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C32LABEL, DBF_STRING) {
        prompt("Column 32 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C33LABEL, DBF_STRING) {
        prompt("Column 33 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C34LABEL, DBF_STRING) {
        prompt("Column 34 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C35LABEL, DBF_STRING) {
        prompt("Column 35 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C36LABEL, DBF_STRING) {
        prompt("Column 36 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C37LABEL, DBF_STRING) {
        prompt("Column 37 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C38LABEL, DBF_STRING) {
        prompt("Column 38 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C39LABEL, DBF_STRING) {
        prompt("Column 39 label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C3ALABEL, DBF_STRING) {
        prompt("Column 3A label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C3BLABEL, DBF_STRING) {
        prompt("Column 3B label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C3CLABEL, DBF_STRING) {
        prompt("Column 3C label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C3DLABEL, DBF_STRING) {
        prompt("Column 3D label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C3ELABEL, DBF_STRING) {
        prompt("Column 3E label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(C3FLABEL, DBF_STRING) {
        prompt("Column 3F label")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    # ----------------------------------------------------------------
    # Column field types - C00TYPE .. C3FTYPE
    # ----------------------------------------------------------------

    field(C00TYPE, DBF_MENU) {
        prompt("Column 00 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C01TYPE, DBF_MENU) {
        prompt("Column 01 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C02TYPE, DBF_MENU) {
        prompt("Column 02 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C03TYPE, DBF_MENU) {
        prompt("Column 03 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C04TYPE, DBF_MENU) {
        prompt("Column 04 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C05TYPE, DBF_MENU) {
        prompt("Column 05 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C06TYPE, DBF_MENU) {
        prompt("Column 06 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C07TYPE, DBF_MENU) {
        prompt("Column 07 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C08TYPE, DBF_MENU) {
        prompt("Column 08 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C09TYPE, DBF_MENU) {
        prompt("Column 09 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C0ATYPE, DBF_MENU) {
        prompt("Column 0A field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C0BTYPE, DBF_MENU) {
        prompt("Column 0B field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C0CTYPE, DBF_MENU) {
        prompt("Column 0C field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C0DTYPE, DBF_MENU) {
        prompt("Column 0D field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C0ETYPE, DBF_MENU) {
        prompt("Column 0E field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C0FTYPE, DBF_MENU) {
        prompt("Column 0F field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C10TYPE, DBF_MENU) {
        prompt("Column 10 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C11TYPE, DBF_MENU) {
        prompt("Column 11 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C12TYPE, DBF_MENU) {
        prompt("Column 12 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C13TYPE, DBF_MENU) {
        prompt("Column 13 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C14TYPE, DBF_MENU) {
        prompt("Column 14 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C15TYPE, DBF_MENU) {
        prompt("Column 15 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C16TYPE, DBF_MENU) {
        prompt("Column 16 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C17TYPE, DBF_MENU) {
        prompt("Column 17 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C18TYPE, DBF_MENU) {
        prompt("Column 18 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C19TYPE, DBF_MENU) {
        prompt("Column 19 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C1ATYPE, DBF_MENU) {
        prompt("Column 1A field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C1BTYPE, DBF_MENU) {
        prompt("Column 1B field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C1CTYPE, DBF_MENU) {
        prompt("Column 1C field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C1DTYPE, DBF_MENU) {
        prompt("Column 1D field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C1ETYPE, DBF_MENU) {
        prompt("Column 1E field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C1FTYPE, DBF_MENU) {
        prompt("Column 1F field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C20TYPE, DBF_MENU) {
        prompt("Column 20 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C21TYPE, DBF_MENU) {
        prompt("Column 21 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C22TYPE, DBF_MENU) {
        prompt("Column 22 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C23TYPE, DBF_MENU) {
        prompt("Column 23 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C24TYPE, DBF_MENU) {
        prompt("Column 24 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C25TYPE, DBF_MENU) {
        prompt("Column 25 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C26TYPE, DBF_MENU) {
        prompt("Column 26 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C27TYPE, DBF_MENU) {
        prompt("Column 27 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C28TYPE, DBF_MENU) {
        prompt("Column 28 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C29TYPE, DBF_MENU) {
        prompt("Column 29 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C2ATYPE, DBF_MENU) {
        prompt("Column 2A field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C2BTYPE, DBF_MENU) {
        prompt("Column 2B field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C2CTYPE, DBF_MENU) {
        prompt("Column 2C field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C2DTYPE, DBF_MENU) {
        prompt("Column 2D field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C2ETYPE, DBF_MENU) {
        prompt("Column 2E field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C2FTYPE, DBF_MENU) {
        prompt("Column 2F field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C30TYPE, DBF_MENU) {
        prompt("Column 30 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C31TYPE, DBF_MENU) {
        prompt("Column 31 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C32TYPE, DBF_MENU) {
        prompt("Column 32 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C33TYPE, DBF_MENU) {
        prompt("Column 33 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C34TYPE, DBF_MENU) {
        prompt("Column 34 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C35TYPE, DBF_MENU) {
        prompt("Column 35 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C36TYPE, DBF_MENU) {
        prompt("Column 36 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C37TYPE, DBF_MENU) {
        prompt("Column 37 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C38TYPE, DBF_MENU) {
        prompt("Column 38 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C39TYPE, DBF_MENU) {
        prompt("Column 39 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C3ATYPE, DBF_MENU) {
        prompt("Column 3A field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C3BTYPE, DBF_MENU) {
        prompt("Column 3B field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C3CTYPE, DBF_MENU) {
        prompt("Column 3C field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C3DTYPE, DBF_MENU) {
        prompt("Column 3D field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C3ETYPE, DBF_MENU) {
        prompt("Column 3E field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(C3FTYPE, DBF_MENU) {
        prompt("Column 3F field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    # ----------------------------------------------------------------
    # Column input links - C00INP .. C3FINP
    # ----------------------------------------------------------------

    field(C00INP, DBF_INLINK) {
        prompt("Column 00 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C01INP, DBF_INLINK) {
        prompt("Column 01 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C02INP, DBF_INLINK) {
        prompt("Column 02 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C03INP, DBF_INLINK) {
        prompt("Column 03 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C04INP, DBF_INLINK) {
        prompt("Column 04 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C05INP, DBF_INLINK) {
        prompt("Column 05 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C06INP, DBF_INLINK) {
        prompt("Column 06 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C07INP, DBF_INLINK) {
        prompt("Column 07 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C08INP, DBF_INLINK) {
        prompt("Column 08 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C09INP, DBF_INLINK) {
        prompt("Column 09 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C0AINP, DBF_INLINK) {
        prompt("Column 0A input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C0BINP, DBF_INLINK) {
        prompt("Column 0B input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C0CINP, DBF_INLINK) {
        prompt("Column 0C input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C0DINP, DBF_INLINK) {
        prompt("Column 0D input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C0EINP, DBF_INLINK) {
        prompt("Column 0E input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C0FINP, DBF_INLINK) {
        prompt("Column 0F input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C10INP, DBF_INLINK) {
        prompt("Column 10 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C11INP, DBF_INLINK) {
        prompt("Column 11 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C12INP, DBF_INLINK) {
        prompt("Column 12 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C13INP, DBF_INLINK) {
        prompt("Column 13 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C14INP, DBF_INLINK) {
        prompt("Column 14 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C15INP, DBF_INLINK) {
        prompt("Column 15 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C16INP, DBF_INLINK) {
        prompt("Column 16 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C17INP, DBF_INLINK) {
        prompt("Column 17 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C18INP, DBF_INLINK) {
        prompt("Column 18 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C19INP, DBF_INLINK) {
        prompt("Column 19 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C1AINP, DBF_INLINK) {
        prompt("Column 1A input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C1BINP, DBF_INLINK) {
        prompt("Column 1B input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C1CINP, DBF_INLINK) {
        prompt("Column 1C input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C1DINP, DBF_INLINK) {
        prompt("Column 1D input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C1EINP, DBF_INLINK) {
        prompt("Column 1E input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C1FINP, DBF_INLINK) {
        prompt("Column 1F input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C20INP, DBF_INLINK) {
        prompt("Column 20 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C21INP, DBF_INLINK) {
        prompt("Column 21 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C22INP, DBF_INLINK) {
        prompt("Column 22 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C23INP, DBF_INLINK) {
        prompt("Column 23 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C24INP, DBF_INLINK) {
        prompt("Column 24 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C25INP, DBF_INLINK) {
        prompt("Column 25 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C26INP, DBF_INLINK) {
        prompt("Column 26 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C27INP, DBF_INLINK) {
        prompt("Column 27 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C28INP, DBF_INLINK) {
        prompt("Column 28 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C29INP, DBF_INLINK) {
        prompt("Column 29 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C2AINP, DBF_INLINK) {
        prompt("Column 2A input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C2BINP, DBF_INLINK) {
        prompt("Column 2B input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C2CINP, DBF_INLINK) {
        prompt("Column 2C input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C2DINP, DBF_INLINK) {
        prompt("Column 2D input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C2EINP, DBF_INLINK) {
        prompt("Column 2E input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C2FINP, DBF_INLINK) {
        prompt("Column 2F input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C30INP, DBF_INLINK) {
        prompt("Column 30 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C31INP, DBF_INLINK) {
        prompt("Column 31 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C32INP, DBF_INLINK) {
        prompt("Column 32 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C33INP, DBF_INLINK) {
        prompt("Column 33 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C34INP, DBF_INLINK) {
        prompt("Column 34 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C35INP, DBF_INLINK) {
        prompt("Column 35 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C36INP, DBF_INLINK) {
        prompt("Column 36 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C37INP, DBF_INLINK) {
        prompt("Column 37 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C38INP, DBF_INLINK) {
        prompt("Column 38 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C39INP, DBF_INLINK) {
        prompt("Column 39 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C3AINP, DBF_INLINK) {
        prompt("Column 3A input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C3BINP, DBF_INLINK) {
        prompt("Column 3B input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C3CINP, DBF_INLINK) {
        prompt("Column 3C input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C3DINP, DBF_INLINK) {
        prompt("Column 3D input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C3EINP, DBF_INLINK) {
        prompt("Column 3E input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(C3FINP, DBF_INLINK) {
        prompt("Column 3F input link")
        promptgroup("41 - Input")
        interest(1)
    }

    # ----------------------------------------------------------------
    # Column data buffers - C00VAL .. C3FVAL
    # ----------------------------------------------------------------

    field(C00VAL, DBF_NOACCESS) {
        prompt("Column 00 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c00val")
        #=type Set by C00TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C01VAL, DBF_NOACCESS) {
        prompt("Column 01 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c01val")
        #=type Set by C01TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C02VAL, DBF_NOACCESS) {
        prompt("Column 02 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c02val")
        #=type Set by C02TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C03VAL, DBF_NOACCESS) {
        prompt("Column 03 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c03val")
        #=type Set by C03TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C04VAL, DBF_NOACCESS) {
        prompt("Column 04 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c04val")
        #=type Set by C04TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C05VAL, DBF_NOACCESS) {
        prompt("Column 05 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c05val")
        #=type Set by C05TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C06VAL, DBF_NOACCESS) {
        prompt("Column 06 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c06val")
        #=type Set by C06TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C07VAL, DBF_NOACCESS) {
        prompt("Column 07 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c07val")
        #=type Set by C07TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C08VAL, DBF_NOACCESS) {
        prompt("Column 08 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c08val")
        #=type Set by C08TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C09VAL, DBF_NOACCESS) {
        prompt("Column 09 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c09val")
        #=type Set by C09TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C0AVAL, DBF_NOACCESS) {
        prompt("Column 0A data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c0aval")
        #=type Set by C0ATYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C0BVAL, DBF_NOACCESS) {
        prompt("Column 0B data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c0bval")
        #=type Set by C0BTYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C0CVAL, DBF_NOACCESS) {
        prompt("Column 0C data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c0cval")
        #=type Set by C0CTYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C0DVAL, DBF_NOACCESS) {
        prompt("Column 0D data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c0dval")
        #=type Set by C0DTYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C0EVAL, DBF_NOACCESS) {
        prompt("Column 0E data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c0eval")
        #=type Set by C0ETYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C0FVAL, DBF_NOACCESS) {
        prompt("Column 0F data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c0fval")
        #=type Set by C0FTYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C10VAL, DBF_NOACCESS) {
        prompt("Column 10 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c10val")
        #=type Set by C10TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C11VAL, DBF_NOACCESS) {
        prompt("Column 11 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c11val")
        #=type Set by C11TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C12VAL, DBF_NOACCESS) {
        prompt("Column 12 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c12val")
        #=type Set by C12TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C13VAL, DBF_NOACCESS) {
        prompt("Column 13 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c13val")
        #=type Set by C13TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C14VAL, DBF_NOACCESS) {
        prompt("Column 14 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c14val")
        #=type Set by C14TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C15VAL, DBF_NOACCESS) {
        prompt("Column 15 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c15val")
        #=type Set by C15TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C16VAL, DBF_NOACCESS) {
        prompt("Column 16 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c16val")
        #=type Set by C16TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C17VAL, DBF_NOACCESS) {
        prompt("Column 17 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c17val")
        #=type Set by C17TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C18VAL, DBF_NOACCESS) {
        prompt("Column 18 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c18val")
        #=type Set by C18TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C19VAL, DBF_NOACCESS) {
        prompt("Column 19 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c19val")
        #=type Set by C19TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C1AVAL, DBF_NOACCESS) {
        prompt("Column 1A data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c1aval")
        #=type Set by C1ATYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C1BVAL, DBF_NOACCESS) {
        prompt("Column 1B data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c1bval")
        #=type Set by C1BTYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C1CVAL, DBF_NOACCESS) {
        prompt("Column 1C data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c1cval")
        #=type Set by C1CTYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C1DVAL, DBF_NOACCESS) {
        prompt("Column 1D data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c1dval")
        #=type Set by C1DTYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C1EVAL, DBF_NOACCESS) {
        prompt("Column 1E data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c1eval")
        #=type Set by C1ETYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C1FVAL, DBF_NOACCESS) {
        prompt("Column 1F data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c1fval")
        #=type Set by C1FTYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C20VAL, DBF_NOACCESS) {
        prompt("Column 20 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c20val")
        #=type Set by C20TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C21VAL, DBF_NOACCESS) {
        prompt("Column 21 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c21val")
        #=type Set by C21TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C22VAL, DBF_NOACCESS) {
        prompt("Column 22 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c22val")
        #=type Set by C22TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C23VAL, DBF_NOACCESS) {
        prompt("Column 23 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c23val")
        #=type Set by C23TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C24VAL, DBF_NOACCESS) {
        prompt("Column 24 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c24val")
        #=type Set by C24TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C25VAL, DBF_NOACCESS) {
        prompt("Column 25 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c25val")
        #=type Set by C25TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C26VAL, DBF_NOACCESS) {
        prompt("Column 26 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c26val")
        #=type Set by C26TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C27VAL, DBF_NOACCESS) {
        prompt("Column 27 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c27val")
        #=type Set by C27TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C28VAL, DBF_NOACCESS) {
        prompt("Column 28 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c28val")
        #=type Set by C28TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C29VAL, DBF_NOACCESS) {
        prompt("Column 29 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c29val")
        #=type Set by C29TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C2AVAL, DBF_NOACCESS) {
        prompt("Column 2A data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c2aval")
        #=type Set by C2ATYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C2BVAL, DBF_NOACCESS) {
        prompt("Column 2B data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c2bval")
        #=type Set by C2BTYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C2CVAL, DBF_NOACCESS) {
        prompt("Column 2C data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c2cval")
        #=type Set by C2CTYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C2DVAL, DBF_NOACCESS) {
        prompt("Column 2D data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c2dval")
        #=type Set by C2DTYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C2EVAL, DBF_NOACCESS) {
        prompt("Column 2E data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c2eval")
        #=type Set by C2ETYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C2FVAL, DBF_NOACCESS) {
        prompt("Column 2F data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c2fval")
        #=type Set by C2FTYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C30VAL, DBF_NOACCESS) {
        prompt("Column 30 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c30val")
        #=type Set by C30TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C31VAL, DBF_NOACCESS) {
        prompt("Column 31 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c31val")
        #=type Set by C31TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C32VAL, DBF_NOACCESS) {
        prompt("Column 32 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c32val")
        #=type Set by C32TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C33VAL, DBF_NOACCESS) {
        prompt("Column 33 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c33val")
        #=type Set by C33TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C34VAL, DBF_NOACCESS) {
        prompt("Column 34 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c34val")
        #=type Set by C34TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C35VAL, DBF_NOACCESS) {
        prompt("Column 35 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c35val")
        #=type Set by C35TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C36VAL, DBF_NOACCESS) {
        prompt("Column 36 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c36val")
        #=type Set by C36TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C37VAL, DBF_NOACCESS) {
        prompt("Column 37 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c37val")
        #=type Set by C37TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C38VAL, DBF_NOACCESS) {
        prompt("Column 38 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c38val")
        #=type Set by C38TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C39VAL, DBF_NOACCESS) {
        prompt("Column 39 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c39val")
        #=type Set by C39TYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C3AVAL, DBF_NOACCESS) {
        prompt("Column 3A data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c3aval")
        #=type Set by C3ATYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C3BVAL, DBF_NOACCESS) {
        prompt("Column 3B data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c3bval")
        #=type Set by C3BTYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C3CVAL, DBF_NOACCESS) {
        prompt("Column 3C data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c3cval")
        #=type Set by C3CTYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C3DVAL, DBF_NOACCESS) {
        prompt("Column 3D data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c3dval")
        #=type Set by C3DTYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C3EVAL, DBF_NOACCESS) {
        prompt("Column 3E data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c3eval")
        #=type Set by C3ETYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(C3FVAL, DBF_NOACCESS) {
        prompt("Column 3F data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *c3fval")
        #=type Set by C3FTYPE[MAXROWS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    # ----------------------------------------------------------------
    # Column changed flags - C00CHGD .. C3FCHGD
    # ----------------------------------------------------------------

    field(C00CHGD, DBF_UCHAR) {
        prompt("Column 00 changed flag")
        interest(2)
    }

    field(C01CHGD, DBF_UCHAR) {
        prompt("Column 01 changed flag")
        interest(2)
    }

    field(C02CHGD, DBF_UCHAR) {
        prompt("Column 02 changed flag")
        interest(2)
    }

    field(C03CHGD, DBF_UCHAR) {
        prompt("Column 03 changed flag")
        interest(2)
    }

    field(C04CHGD, DBF_UCHAR) {
        prompt("Column 04 changed flag")
        interest(2)
    }

    field(C05CHGD, DBF_UCHAR) {
        prompt("Column 05 changed flag")
        interest(2)
    }

    field(C06CHGD, DBF_UCHAR) {
        prompt("Column 06 changed flag")
        interest(2)
    }

    field(C07CHGD, DBF_UCHAR) {
        prompt("Column 07 changed flag")
        interest(2)
    }

    field(C08CHGD, DBF_UCHAR) {
        prompt("Column 08 changed flag")
        interest(2)
    }

    field(C09CHGD, DBF_UCHAR) {
        prompt("Column 09 changed flag")
        interest(2)
    }

    field(C0ACHGD, DBF_UCHAR) {
        prompt("Column 0A changed flag")
        interest(2)
    }

    field(C0BCHGD, DBF_UCHAR) {
        prompt("Column 0B changed flag")
        interest(2)
    }

    field(C0CCHGD, DBF_UCHAR) {
        prompt("Column 0C changed flag")
        interest(2)
    }

    field(C0DCHGD, DBF_UCHAR) {
        prompt("Column 0D changed flag")
        interest(2)
    }

    field(C0ECHGD, DBF_UCHAR) {
        prompt("Column 0E changed flag")
        interest(2)
    }

    field(C0FCHGD, DBF_UCHAR) {
        prompt("Column 0F changed flag")
        interest(2)
    }

    field(C10CHGD, DBF_UCHAR) {
        prompt("Column 10 changed flag")
        interest(2)
    }

    field(C11CHGD, DBF_UCHAR) {
        prompt("Column 11 changed flag")
        interest(2)
    }

    field(C12CHGD, DBF_UCHAR) {
        prompt("Column 12 changed flag")
        interest(2)
    }

    field(C13CHGD, DBF_UCHAR) {
        prompt("Column 13 changed flag")
        interest(2)
    }

    field(C14CHGD, DBF_UCHAR) {
        prompt("Column 14 changed flag")
        interest(2)
    }

    field(C15CHGD, DBF_UCHAR) {
        prompt("Column 15 changed flag")
        interest(2)
    }

    field(C16CHGD, DBF_UCHAR) {
        prompt("Column 16 changed flag")
        interest(2)
    }

    field(C17CHGD, DBF_UCHAR) {
        prompt("Column 17 changed flag")
        interest(2)
    }

    field(C18CHGD, DBF_UCHAR) {
        prompt("Column 18 changed flag")
        interest(2)
    }

    field(C19CHGD, DBF_UCHAR) {
        prompt("Column 19 changed flag")
        interest(2)
    }

    field(C1ACHGD, DBF_UCHAR) {
        prompt("Column 1A changed flag")
        interest(2)
    }

    field(C1BCHGD, DBF_UCHAR) {
        prompt("Column 1B changed flag")
        interest(2)
    }

    field(C1CCHGD, DBF_UCHAR) {
        prompt("Column 1C changed flag")
        interest(2)
    }

    field(C1DCHGD, DBF_UCHAR) {
        prompt("Column 1D changed flag")
        interest(2)
    }

    field(C1ECHGD, DBF_UCHAR) {
        prompt("Column 1E changed flag")
        interest(2)
    }

    field(C1FCHGD, DBF_UCHAR) {
        prompt("Column 1F changed flag")
        interest(2)
    }

    field(C20CHGD, DBF_UCHAR) {
        prompt("Column 20 changed flag")
        interest(2)
    }

    field(C21CHGD, DBF_UCHAR) {
        prompt("Column 21 changed flag")
        interest(2)
    }

    field(C22CHGD, DBF_UCHAR) {
        prompt("Column 22 changed flag")
        interest(2)
    }

    field(C23CHGD, DBF_UCHAR) {
        prompt("Column 23 changed flag")
        interest(2)
    }

    field(C24CHGD, DBF_UCHAR) {
        prompt("Column 24 changed flag")
        interest(2)
    }

    field(C25CHGD, DBF_UCHAR) {
        prompt("Column 25 changed flag")
        interest(2)
    }

    field(C26CHGD, DBF_UCHAR) {
        prompt("Column 26 changed flag")
        interest(2)
    }

    field(C27CHGD, DBF_UCHAR) {
        prompt("Column 27 changed flag")
        interest(2)
    }

    field(C28CHGD, DBF_UCHAR) {
        prompt("Column 28 changed flag")
        interest(2)
    }

    field(C29CHGD, DBF_UCHAR) {
        prompt("Column 29 changed flag")
        interest(2)
    }

    field(C2ACHGD, DBF_UCHAR) {
        prompt("Column 2A changed flag")
        interest(2)
    }

    field(C2BCHGD, DBF_UCHAR) {
        prompt("Column 2B changed flag")
        interest(2)
    }

    field(C2CCHGD, DBF_UCHAR) {
        prompt("Column 2C changed flag")
        interest(2)
    }

    field(C2DCHGD, DBF_UCHAR) {
        prompt("Column 2D changed flag")
        interest(2)
    }

    field(C2ECHGD, DBF_UCHAR) {
        prompt("Column 2E changed flag")
        interest(2)
    }

    field(C2FCHGD, DBF_UCHAR) {
        prompt("Column 2F changed flag")
        interest(2)
    }

    field(C30CHGD, DBF_UCHAR) {
        prompt("Column 30 changed flag")
        interest(2)
    }

    field(C31CHGD, DBF_UCHAR) {
        prompt("Column 31 changed flag")
        interest(2)
    }

    field(C32CHGD, DBF_UCHAR) {
        prompt("Column 32 changed flag")
        interest(2)
    }

    field(C33CHGD, DBF_UCHAR) {
        prompt("Column 33 changed flag")
        interest(2)
    }

    field(C34CHGD, DBF_UCHAR) {
        prompt("Column 34 changed flag")
        interest(2)
    }

    field(C35CHGD, DBF_UCHAR) {
        prompt("Column 35 changed flag")
        interest(2)
    }

    field(C36CHGD, DBF_UCHAR) {
        prompt("Column 36 changed flag")
        interest(2)
    }

    field(C37CHGD, DBF_UCHAR) {
        prompt("Column 37 changed flag")
        interest(2)
    }

    field(C38CHGD, DBF_UCHAR) {
        prompt("Column 38 changed flag")
        interest(2)
    }

    field(C39CHGD, DBF_UCHAR) {
        prompt("Column 39 changed flag")
        interest(2)
    }

    field(C3ACHGD, DBF_UCHAR) {
        prompt("Column 3A changed flag")
        interest(2)
    }

    field(C3BCHGD, DBF_UCHAR) {
        prompt("Column 3B changed flag")
        interest(2)
    }

    field(C3CCHGD, DBF_UCHAR) {
        prompt("Column 3C changed flag")
        interest(2)
    }

    field(C3DCHGD, DBF_UCHAR) {
        prompt("Column 3D changed flag")
        interest(2)
    }

    field(C3ECHGD, DBF_UCHAR) {
        prompt("Column 3E changed flag")
        interest(2)
    }

    field(C3FCHGD, DBF_UCHAR) {
        prompt("Column 3F changed flag")
        interest(2)
    }

    # ----------------------------------------------------------------
    # Column valid row counts - C00NROWS .. C3FNROWS
    # ----------------------------------------------------------------

    field(C00NROWS, DBF_ULONG) {
        prompt("Column 00 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C01NROWS, DBF_ULONG) {
        prompt("Column 01 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C02NROWS, DBF_ULONG) {
        prompt("Column 02 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C03NROWS, DBF_ULONG) {
        prompt("Column 03 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C04NROWS, DBF_ULONG) {
        prompt("Column 04 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C05NROWS, DBF_ULONG) {
        prompt("Column 05 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C06NROWS, DBF_ULONG) {
        prompt("Column 06 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C07NROWS, DBF_ULONG) {
        prompt("Column 07 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C08NROWS, DBF_ULONG) {
        prompt("Column 08 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C09NROWS, DBF_ULONG) {
        prompt("Column 09 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C0ANROWS, DBF_ULONG) {
        prompt("Column 0A number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C0BNROWS, DBF_ULONG) {
        prompt("Column 0B number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C0CNROWS, DBF_ULONG) {
        prompt("Column 0C number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C0DNROWS, DBF_ULONG) {
        prompt("Column 0D number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C0ENROWS, DBF_ULONG) {
        prompt("Column 0E number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C0FNROWS, DBF_ULONG) {
        prompt("Column 0F number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C10NROWS, DBF_ULONG) {
        prompt("Column 10 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C11NROWS, DBF_ULONG) {
        prompt("Column 11 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C12NROWS, DBF_ULONG) {
        prompt("Column 12 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C13NROWS, DBF_ULONG) {
        prompt("Column 13 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C14NROWS, DBF_ULONG) {
        prompt("Column 14 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C15NROWS, DBF_ULONG) {
        prompt("Column 15 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C16NROWS, DBF_ULONG) {
        prompt("Column 16 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C17NROWS, DBF_ULONG) {
        prompt("Column 17 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C18NROWS, DBF_ULONG) {
        prompt("Column 18 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C19NROWS, DBF_ULONG) {
        prompt("Column 19 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C1ANROWS, DBF_ULONG) {
        prompt("Column 1A number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C1BNROWS, DBF_ULONG) {
        prompt("Column 1B number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C1CNROWS, DBF_ULONG) {
        prompt("Column 1C number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C1DNROWS, DBF_ULONG) {
        prompt("Column 1D number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C1ENROWS, DBF_ULONG) {
        prompt("Column 1E number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C1FNROWS, DBF_ULONG) {
        prompt("Column 1F number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C20NROWS, DBF_ULONG) {
        prompt("Column 20 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C21NROWS, DBF_ULONG) {
        prompt("Column 21 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C22NROWS, DBF_ULONG) {
        prompt("Column 22 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C23NROWS, DBF_ULONG) {
        prompt("Column 23 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C24NROWS, DBF_ULONG) {
        prompt("Column 24 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C25NROWS, DBF_ULONG) {
        prompt("Column 25 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C26NROWS, DBF_ULONG) {
        prompt("Column 26 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C27NROWS, DBF_ULONG) {
        prompt("Column 27 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C28NROWS, DBF_ULONG) {
        prompt("Column 28 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C29NROWS, DBF_ULONG) {
        prompt("Column 29 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C2ANROWS, DBF_ULONG) {
        prompt("Column 2A number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C2BNROWS, DBF_ULONG) {
        prompt("Column 2B number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C2CNROWS, DBF_ULONG) {
        prompt("Column 2C number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C2DNROWS, DBF_ULONG) {
        prompt("Column 2D number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C2ENROWS, DBF_ULONG) {
        prompt("Column 2E number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C2FNROWS, DBF_ULONG) {
        prompt("Column 2F number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C30NROWS, DBF_ULONG) {
        prompt("Column 30 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C31NROWS, DBF_ULONG) {
        prompt("Column 31 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C32NROWS, DBF_ULONG) {
        prompt("Column 32 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C33NROWS, DBF_ULONG) {
        prompt("Column 33 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C34NROWS, DBF_ULONG) {
        prompt("Column 34 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C35NROWS, DBF_ULONG) {
        prompt("Column 35 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C36NROWS, DBF_ULONG) {
        prompt("Column 36 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C37NROWS, DBF_ULONG) {
        prompt("Column 37 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C38NROWS, DBF_ULONG) {
        prompt("Column 38 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C39NROWS, DBF_ULONG) {
        prompt("Column 39 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C3ANROWS, DBF_ULONG) {
        prompt("Column 3A number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C3BNROWS, DBF_ULONG) {
        prompt("Column 3B number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C3CNROWS, DBF_ULONG) {
        prompt("Column 3C number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C3DNROWS, DBF_ULONG) {
        prompt("Column 3D number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C3ENROWS, DBF_ULONG) {
        prompt("Column 3E number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(C3FNROWS, DBF_ULONG) {
        prompt("Column 3F number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    # ----------------------------------------------------------------
    # Optional column names - CO00NAME .. CO3FNAME
    # ----------------------------------------------------------------

    field(CO00NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 00 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO01NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 01 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO02NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 02 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO03NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 03 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO04NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 04 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO05NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 05 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO06NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 06 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO07NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 07 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO08NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 08 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO09NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 09 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO0ANAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 0A name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO0BNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 0B name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO0CNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 0C name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO0DNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 0D name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO0ENAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 0E name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO0FNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 0F name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO10NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 10 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO11NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 11 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO12NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 12 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO13NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 13 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO14NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 14 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO15NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 15 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO16NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 16 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO17NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 17 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO18NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 18 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO19NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 19 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO1ANAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 1A name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO1BNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 1B name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO1CNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 1C name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO1DNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 1D name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO1ENAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 1E name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO1FNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 1F name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO20NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 20 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO21NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 21 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO22NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 22 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO23NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 23 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO24NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 24 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO25NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 25 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO26NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 26 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO27NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 27 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO28NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 28 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO29NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 29 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO2ANAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 2A name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO2BNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 2B name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO2CNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 2C name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO2DNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 2D name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO2ENAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 2E name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO2FNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 2F name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO30NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 30 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO31NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 31 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO32NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 32 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO33NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 33 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO34NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 34 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO35NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 35 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO36NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 36 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO37NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 37 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO38NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 38 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO39NAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 39 name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO3ANAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 3A name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO3BNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 3B name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO3CNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 3C name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO3DNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 3D name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO3ENAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 3E name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    field(CO3FNAME, DBF_STRING) {
        special(SPC_NOMOD)
        prompt("Optional column 3F name")
        promptgroup("41 - Input")
        interest(1)
        size(40)
    }

    # ----------------------------------------------------------------
    # Optional column field types - CO00TYPE .. CO3FTYPE
    # ----------------------------------------------------------------

    field(CO00TYPE, DBF_MENU) {
        prompt("Optional column 00 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO01TYPE, DBF_MENU) {
        prompt("Optional column 01 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO02TYPE, DBF_MENU) {
        prompt("Optional column 02 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO03TYPE, DBF_MENU) {
        prompt("Optional column 03 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO04TYPE, DBF_MENU) {
        prompt("Optional column 04 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO05TYPE, DBF_MENU) {
        prompt("Optional column 05 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO06TYPE, DBF_MENU) {
        prompt("Optional column 06 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO07TYPE, DBF_MENU) {
        prompt("Optional column 07 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO08TYPE, DBF_MENU) {
        prompt("Optional column 08 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO09TYPE, DBF_MENU) {
        prompt("Optional column 09 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO0ATYPE, DBF_MENU) {
        prompt("Optional column 0A field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO0BTYPE, DBF_MENU) {
        prompt("Optional column 0B field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO0CTYPE, DBF_MENU) {
        prompt("Optional column 0C field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO0DTYPE, DBF_MENU) {
        prompt("Optional column 0D field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO0ETYPE, DBF_MENU) {
        prompt("Optional column 0E field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO0FTYPE, DBF_MENU) {
        prompt("Optional column 0F field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO10TYPE, DBF_MENU) {
        prompt("Optional column 10 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO11TYPE, DBF_MENU) {
        prompt("Optional column 11 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO12TYPE, DBF_MENU) {
        prompt("Optional column 12 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO13TYPE, DBF_MENU) {
        prompt("Optional column 13 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO14TYPE, DBF_MENU) {
        prompt("Optional column 14 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO15TYPE, DBF_MENU) {
        prompt("Optional column 15 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO16TYPE, DBF_MENU) {
        prompt("Optional column 16 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO17TYPE, DBF_MENU) {
        prompt("Optional column 17 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO18TYPE, DBF_MENU) {
        prompt("Optional column 18 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO19TYPE, DBF_MENU) {
        prompt("Optional column 19 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO1ATYPE, DBF_MENU) {
        prompt("Optional column 1A field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO1BTYPE, DBF_MENU) {
        prompt("Optional column 1B field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO1CTYPE, DBF_MENU) {
        prompt("Optional column 1C field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO1DTYPE, DBF_MENU) {
        prompt("Optional column 1D field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO1ETYPE, DBF_MENU) {
        prompt("Optional column 1E field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO1FTYPE, DBF_MENU) {
        prompt("Optional column 1F field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO20TYPE, DBF_MENU) {
        prompt("Optional column 20 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO21TYPE, DBF_MENU) {
        prompt("Optional column 21 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO22TYPE, DBF_MENU) {
        prompt("Optional column 22 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO23TYPE, DBF_MENU) {
        prompt("Optional column 23 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO24TYPE, DBF_MENU) {
        prompt("Optional column 24 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO25TYPE, DBF_MENU) {
        prompt("Optional column 25 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO26TYPE, DBF_MENU) {
        prompt("Optional column 26 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO27TYPE, DBF_MENU) {
        prompt("Optional column 27 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO28TYPE, DBF_MENU) {
        prompt("Optional column 28 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO29TYPE, DBF_MENU) {
        prompt("Optional column 29 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO2ATYPE, DBF_MENU) {
        prompt("Optional column 2A field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO2BTYPE, DBF_MENU) {
        prompt("Optional column 2B field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO2CTYPE, DBF_MENU) {
        prompt("Optional column 2C field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO2DTYPE, DBF_MENU) {
        prompt("Optional column 2D field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO2ETYPE, DBF_MENU) {
        prompt("Optional column 2E field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO2FTYPE, DBF_MENU) {
        prompt("Optional column 2F field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO30TYPE, DBF_MENU) {
        prompt("Optional column 30 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO31TYPE, DBF_MENU) {
        prompt("Optional column 31 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO32TYPE, DBF_MENU) {
        prompt("Optional column 32 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO33TYPE, DBF_MENU) {
        prompt("Optional column 33 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO34TYPE, DBF_MENU) {
        prompt("Optional column 34 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO35TYPE, DBF_MENU) {
        prompt("Optional column 35 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO36TYPE, DBF_MENU) {
        prompt("Optional column 36 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO37TYPE, DBF_MENU) {
        prompt("Optional column 37 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO38TYPE, DBF_MENU) {
        prompt("Optional column 38 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO39TYPE, DBF_MENU) {
        prompt("Optional column 39 field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO3ATYPE, DBF_MENU) {
        prompt("Optional column 3A field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO3BTYPE, DBF_MENU) {
        prompt("Optional column 3B field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO3CTYPE, DBF_MENU) {
        prompt("Optional column 3C field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO3DTYPE, DBF_MENU) {
        prompt("Optional column 3D field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO3ETYPE, DBF_MENU) {
        prompt("Optional column 3E field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    field(CO3FTYPE, DBF_MENU) {
        prompt("Optional column 3F field type")
        promptgroup("41 - Input")
        special(SPC_NOMOD)
        interest(1)
        menu(menuFtype)
        initial("DOUBLE")
    }

    # ----------------------------------------------------------------
    # Optional column input links - CO00INP .. CO3FINP
    # ----------------------------------------------------------------

    field(CO00INP, DBF_INLINK) {
        prompt("Optional column 00 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO01INP, DBF_INLINK) {
        prompt("Optional column 01 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO02INP, DBF_INLINK) {
        prompt("Optional column 02 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO03INP, DBF_INLINK) {
        prompt("Optional column 03 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO04INP, DBF_INLINK) {
        prompt("Optional column 04 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO05INP, DBF_INLINK) {
        prompt("Optional column 05 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO06INP, DBF_INLINK) {
        prompt("Optional column 06 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO07INP, DBF_INLINK) {
        prompt("Optional column 07 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO08INP, DBF_INLINK) {
        prompt("Optional column 08 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO09INP, DBF_INLINK) {
        prompt("Optional column 09 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO0AINP, DBF_INLINK) {
        prompt("Optional column 0A input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO0BINP, DBF_INLINK) {
        prompt("Optional column 0B input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO0CINP, DBF_INLINK) {
        prompt("Optional column 0C input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO0DINP, DBF_INLINK) {
        prompt("Optional column 0D input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO0EINP, DBF_INLINK) {
        prompt("Optional column 0E input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO0FINP, DBF_INLINK) {
        prompt("Optional column 0F input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO10INP, DBF_INLINK) {
        prompt("Optional column 10 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO11INP, DBF_INLINK) {
        prompt("Optional column 11 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO12INP, DBF_INLINK) {
        prompt("Optional column 12 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO13INP, DBF_INLINK) {
        prompt("Optional column 13 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO14INP, DBF_INLINK) {
        prompt("Optional column 14 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO15INP, DBF_INLINK) {
        prompt("Optional column 15 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO16INP, DBF_INLINK) {
        prompt("Optional column 16 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO17INP, DBF_INLINK) {
        prompt("Optional column 17 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO18INP, DBF_INLINK) {
        prompt("Optional column 18 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO19INP, DBF_INLINK) {
        prompt("Optional column 19 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO1AINP, DBF_INLINK) {
        prompt("Optional column 1A input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO1BINP, DBF_INLINK) {
        prompt("Optional column 1B input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO1CINP, DBF_INLINK) {
        prompt("Optional column 1C input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO1DINP, DBF_INLINK) {
        prompt("Optional column 1D input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO1EINP, DBF_INLINK) {
        prompt("Optional column 1E input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO1FINP, DBF_INLINK) {
        prompt("Optional column 1F input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO20INP, DBF_INLINK) {
        prompt("Optional column 20 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO21INP, DBF_INLINK) {
        prompt("Optional column 21 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO22INP, DBF_INLINK) {
        prompt("Optional column 22 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO23INP, DBF_INLINK) {
        prompt("Optional column 23 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO24INP, DBF_INLINK) {
        prompt("Optional column 24 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO25INP, DBF_INLINK) {
        prompt("Optional column 25 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO26INP, DBF_INLINK) {
        prompt("Optional column 26 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO27INP, DBF_INLINK) {
        prompt("Optional column 27 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO28INP, DBF_INLINK) {
        prompt("Optional column 28 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO29INP, DBF_INLINK) {
        prompt("Optional column 29 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO2AINP, DBF_INLINK) {
        prompt("Optional column 2A input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO2BINP, DBF_INLINK) {
        prompt("Optional column 2B input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO2CINP, DBF_INLINK) {
        prompt("Optional column 2C input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO2DINP, DBF_INLINK) {
        prompt("Optional column 2D input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO2EINP, DBF_INLINK) {
        prompt("Optional column 2E input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO2FINP, DBF_INLINK) {
        prompt("Optional column 2F input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO30INP, DBF_INLINK) {
        prompt("Optional column 30 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO31INP, DBF_INLINK) {
        prompt("Optional column 31 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO32INP, DBF_INLINK) {
        prompt("Optional column 32 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO33INP, DBF_INLINK) {
        prompt("Optional column 33 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO34INP, DBF_INLINK) {
        prompt("Optional column 34 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO35INP, DBF_INLINK) {
        prompt("Optional column 35 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO36INP, DBF_INLINK) {
        prompt("Optional column 36 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO37INP, DBF_INLINK) {
        prompt("Optional column 37 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO38INP, DBF_INLINK) {
        prompt("Optional column 38 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO39INP, DBF_INLINK) {
        prompt("Optional column 39 input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO3AINP, DBF_INLINK) {
        prompt("Optional column 3A input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO3BINP, DBF_INLINK) {
        prompt("Optional column 3B input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO3CINP, DBF_INLINK) {
        prompt("Optional column 3C input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO3DINP, DBF_INLINK) {
        prompt("Optional column 3D input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO3EINP, DBF_INLINK) {
        prompt("Optional column 3E input link")
        promptgroup("41 - Input")
        interest(1)
    }

    field(CO3FINP, DBF_INLINK) {
        prompt("Optional column 3F input link")
        promptgroup("41 - Input")
        interest(1)
    }

    # ----------------------------------------------------------------
    # Optional column data buffers - CO00VAL .. CO3FVAL
    # ----------------------------------------------------------------

    field(CO00VAL, DBF_NOACCESS) {
        prompt("Optional column 00 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co00val")
        #=type Set by CO00TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO01VAL, DBF_NOACCESS) {
        prompt("Optional column 01 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co01val")
        #=type Set by CO01TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO02VAL, DBF_NOACCESS) {
        prompt("Optional column 02 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co02val")
        #=type Set by CO02TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO03VAL, DBF_NOACCESS) {
        prompt("Optional column 03 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co03val")
        #=type Set by CO03TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO04VAL, DBF_NOACCESS) {
        prompt("Optional column 04 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co04val")
        #=type Set by CO04TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO05VAL, DBF_NOACCESS) {
        prompt("Optional column 05 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co05val")
        #=type Set by CO05TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO06VAL, DBF_NOACCESS) {
        prompt("Optional column 06 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co06val")
        #=type Set by CO06TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO07VAL, DBF_NOACCESS) {
        prompt("Optional column 07 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co07val")
        #=type Set by CO07TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO08VAL, DBF_NOACCESS) {
        prompt("Optional column 08 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co08val")
        #=type Set by CO08TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO09VAL, DBF_NOACCESS) {
        prompt("Optional column 09 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co09val")
        #=type Set by CO09TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO0AVAL, DBF_NOACCESS) {
        prompt("Optional column 0A data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co0aval")
        #=type Set by CO0ATYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO0BVAL, DBF_NOACCESS) {
        prompt("Optional column 0B data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co0bval")
        #=type Set by CO0BTYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO0CVAL, DBF_NOACCESS) {
        prompt("Optional column 0C data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co0cval")
        #=type Set by CO0CTYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO0DVAL, DBF_NOACCESS) {
        prompt("Optional column 0D data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co0dval")
        #=type Set by CO0DTYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO0EVAL, DBF_NOACCESS) {
        prompt("Optional column 0E data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co0eval")
        #=type Set by CO0ETYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO0FVAL, DBF_NOACCESS) {
        prompt("Optional column 0F data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co0fval")
        #=type Set by CO0FTYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO10VAL, DBF_NOACCESS) {
        prompt("Optional column 10 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co10val")
        #=type Set by CO10TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO11VAL, DBF_NOACCESS) {
        prompt("Optional column 11 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co11val")
        #=type Set by CO11TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO12VAL, DBF_NOACCESS) {
        prompt("Optional column 12 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co12val")
        #=type Set by CO12TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO13VAL, DBF_NOACCESS) {
        prompt("Optional column 13 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co13val")
        #=type Set by CO13TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO14VAL, DBF_NOACCESS) {
        prompt("Optional column 14 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co14val")
        #=type Set by CO14TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO15VAL, DBF_NOACCESS) {
        prompt("Optional column 15 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co15val")
        #=type Set by CO15TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO16VAL, DBF_NOACCESS) {
        prompt("Optional column 16 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co16val")
        #=type Set by CO16TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO17VAL, DBF_NOACCESS) {
        prompt("Optional column 17 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co17val")
        #=type Set by CO17TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO18VAL, DBF_NOACCESS) {
        prompt("Optional column 18 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co18val")
        #=type Set by CO18TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO19VAL, DBF_NOACCESS) {
        prompt("Optional column 19 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co19val")
        #=type Set by CO19TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO1AVAL, DBF_NOACCESS) {
        prompt("Optional column 1A data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co1aval")
        #=type Set by CO1ATYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO1BVAL, DBF_NOACCESS) {
        prompt("Optional column 1B data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co1bval")
        #=type Set by CO1BTYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO1CVAL, DBF_NOACCESS) {
        prompt("Optional column 1C data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co1cval")
        #=type Set by CO1CTYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO1DVAL, DBF_NOACCESS) {
        prompt("Optional column 1D data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co1dval")
        #=type Set by CO1DTYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO1EVAL, DBF_NOACCESS) {
        prompt("Optional column 1E data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co1eval")
        #=type Set by CO1ETYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO1FVAL, DBF_NOACCESS) {
        prompt("Optional column 1F data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co1fval")
        #=type Set by CO1FTYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO20VAL, DBF_NOACCESS) {
        prompt("Optional column 20 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co20val")
        #=type Set by CO20TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO21VAL, DBF_NOACCESS) {
        prompt("Optional column 21 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co21val")
        #=type Set by CO21TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO22VAL, DBF_NOACCESS) {
        prompt("Optional column 22 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co22val")
        #=type Set by CO22TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO23VAL, DBF_NOACCESS) {
        prompt("Optional column 23 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co23val")
        #=type Set by CO23TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO24VAL, DBF_NOACCESS) {
        prompt("Optional column 24 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co24val")
        #=type Set by CO24TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO25VAL, DBF_NOACCESS) {
        prompt("Optional column 25 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co25val")
        #=type Set by CO25TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO26VAL, DBF_NOACCESS) {
        prompt("Optional column 26 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co26val")
        #=type Set by CO26TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO27VAL, DBF_NOACCESS) {
        prompt("Optional column 27 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co27val")
        #=type Set by CO27TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO28VAL, DBF_NOACCESS) {
        prompt("Optional column 28 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co28val")
        #=type Set by CO28TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO29VAL, DBF_NOACCESS) {
        prompt("Optional column 29 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co29val")
        #=type Set by CO29TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO2AVAL, DBF_NOACCESS) {
        prompt("Optional column 2A data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co2aval")
        #=type Set by CO2ATYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO2BVAL, DBF_NOACCESS) {
        prompt("Optional column 2B data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co2bval")
        #=type Set by CO2BTYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO2CVAL, DBF_NOACCESS) {
        prompt("Optional column 2C data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co2cval")
        #=type Set by CO2CTYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO2DVAL, DBF_NOACCESS) {
        prompt("Optional column 2D data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co2dval")
        #=type Set by CO2DTYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO2EVAL, DBF_NOACCESS) {
        prompt("Optional column 2E data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co2eval")
        #=type Set by CO2ETYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO2FVAL, DBF_NOACCESS) {
        prompt("Optional column 2F data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co2fval")
        #=type Set by CO2FTYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO30VAL, DBF_NOACCESS) {
        prompt("Optional column 30 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co30val")
        #=type Set by CO30TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO31VAL, DBF_NOACCESS) {
        prompt("Optional column 31 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co31val")
        #=type Set by CO31TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO32VAL, DBF_NOACCESS) {
        prompt("Optional column 32 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co32val")
        #=type Set by CO32TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO33VAL, DBF_NOACCESS) {
        prompt("Optional column 33 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co33val")
        #=type Set by CO33TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO34VAL, DBF_NOACCESS) {
        prompt("Optional column 34 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co34val")
        #=type Set by CO34TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO35VAL, DBF_NOACCESS) {
        prompt("Optional column 35 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co35val")
        #=type Set by CO35TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO36VAL, DBF_NOACCESS) {
        prompt("Optional column 36 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co36val")
        #=type Set by CO36TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO37VAL, DBF_NOACCESS) {
        prompt("Optional column 37 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co37val")
        #=type Set by CO37TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO38VAL, DBF_NOACCESS) {
        prompt("Optional column 38 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co38val")
        #=type Set by CO38TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO39VAL, DBF_NOACCESS) {
        prompt("Optional column 39 data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co39val")
        #=type Set by CO39TYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO3AVAL, DBF_NOACCESS) {
        prompt("Optional column 3A data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co3aval")
        #=type Set by CO3ATYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO3BVAL, DBF_NOACCESS) {
        prompt("Optional column 3B data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co3bval")
        #=type Set by CO3BTYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO3CVAL, DBF_NOACCESS) {
        prompt("Optional column 3C data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co3cval")
        #=type Set by CO3CTYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO3DVAL, DBF_NOACCESS) {
        prompt("Optional column 3D data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co3dval")
        #=type Set by CO3DTYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO3EVAL, DBF_NOACCESS) {
        prompt("Optional column 3E data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co3eval")
        #=type Set by CO3ETYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(CO3FVAL, DBF_NOACCESS) {
        prompt("Optional column 3F data")
        special(SPC_DBADDR)
        pp(TRUE)
        extra("void *co3fval")
        #=type Set by CO3FTYPE[NUMCOLS]
        #=read Yes
        #=write Yes
        interest(2)
    }

    field(NUMOPTCOLS, DBF_ULONG) {
        prompt("Number of active optional columns (set by device support)")
        special(SPC_NOMOD)
        interest(1)
    }

    # ----------------------------------------------------------------
    # Optional column changed flags - CO00CHGD .. CO3FCHGD
    # ----------------------------------------------------------------

    field(CO00CHGD, DBF_UCHAR) {
        prompt("Optional column 00 changed flag")
        interest(2)
    }

    field(CO01CHGD, DBF_UCHAR) {
        prompt("Optional column 01 changed flag")
        interest(2)
    }

    field(CO02CHGD, DBF_UCHAR) {
        prompt("Optional column 02 changed flag")
        interest(2)
    }

    field(CO03CHGD, DBF_UCHAR) {
        prompt("Optional column 03 changed flag")
        interest(2)
    }

    field(CO04CHGD, DBF_UCHAR) {
        prompt("Optional column 04 changed flag")
        interest(2)
    }

    field(CO05CHGD, DBF_UCHAR) {
        prompt("Optional column 05 changed flag")
        interest(2)
    }

    field(CO06CHGD, DBF_UCHAR) {
        prompt("Optional column 06 changed flag")
        interest(2)
    }

    field(CO07CHGD, DBF_UCHAR) {
        prompt("Optional column 07 changed flag")
        interest(2)
    }

    field(CO08CHGD, DBF_UCHAR) {
        prompt("Optional column 08 changed flag")
        interest(2)
    }

    field(CO09CHGD, DBF_UCHAR) {
        prompt("Optional column 09 changed flag")
        interest(2)
    }

    field(CO0ACHGD, DBF_UCHAR) {
        prompt("Optional column 0A changed flag")
        interest(2)
    }

    field(CO0BCHGD, DBF_UCHAR) {
        prompt("Optional column 0B changed flag")
        interest(2)
    }

    field(CO0CCHGD, DBF_UCHAR) {
        prompt("Optional column 0C changed flag")
        interest(2)
    }

    field(CO0DCHGD, DBF_UCHAR) {
        prompt("Optional column 0D changed flag")
        interest(2)
    }

    field(CO0ECHGD, DBF_UCHAR) {
        prompt("Optional column 0E changed flag")
        interest(2)
    }

    field(CO0FCHGD, DBF_UCHAR) {
        prompt("Optional column 0F changed flag")
        interest(2)
    }

    field(CO10CHGD, DBF_UCHAR) {
        prompt("Optional column 10 changed flag")
        interest(2)
    }

    field(CO11CHGD, DBF_UCHAR) {
        prompt("Optional column 11 changed flag")
        interest(2)
    }

    field(CO12CHGD, DBF_UCHAR) {
        prompt("Optional column 12 changed flag")
        interest(2)
    }

    field(CO13CHGD, DBF_UCHAR) {
        prompt("Optional column 13 changed flag")
        interest(2)
    }

    field(CO14CHGD, DBF_UCHAR) {
        prompt("Optional column 14 changed flag")
        interest(2)
    }

    field(CO15CHGD, DBF_UCHAR) {
        prompt("Optional column 15 changed flag")
        interest(2)
    }

    field(CO16CHGD, DBF_UCHAR) {
        prompt("Optional column 16 changed flag")
        interest(2)
    }

    field(CO17CHGD, DBF_UCHAR) {
        prompt("Optional column 17 changed flag")
        interest(2)
    }

    field(CO18CHGD, DBF_UCHAR) {
        prompt("Optional column 18 changed flag")
        interest(2)
    }

    field(CO19CHGD, DBF_UCHAR) {
        prompt("Optional column 19 changed flag")
        interest(2)
    }

    field(CO1ACHGD, DBF_UCHAR) {
        prompt("Optional column 1A changed flag")
        interest(2)
    }

    field(CO1BCHGD, DBF_UCHAR) {
        prompt("Optional column 1B changed flag")
        interest(2)
    }

    field(CO1CCHGD, DBF_UCHAR) {
        prompt("Optional column 1C changed flag")
        interest(2)
    }

    field(CO1DCHGD, DBF_UCHAR) {
        prompt("Optional column 1D changed flag")
        interest(2)
    }

    field(CO1ECHGD, DBF_UCHAR) {
        prompt("Optional column 1E changed flag")
        interest(2)
    }

    field(CO1FCHGD, DBF_UCHAR) {
        prompt("Optional column 1F changed flag")
        interest(2)
    }

    field(CO20CHGD, DBF_UCHAR) {
        prompt("Optional column 20 changed flag")
        interest(2)
    }

    field(CO21CHGD, DBF_UCHAR) {
        prompt("Optional column 21 changed flag")
        interest(2)
    }

    field(CO22CHGD, DBF_UCHAR) {
        prompt("Optional column 22 changed flag")
        interest(2)
    }

    field(CO23CHGD, DBF_UCHAR) {
        prompt("Optional column 23 changed flag")
        interest(2)
    }

    field(CO24CHGD, DBF_UCHAR) {
        prompt("Optional column 24 changed flag")
        interest(2)
    }

    field(CO25CHGD, DBF_UCHAR) {
        prompt("Optional column 25 changed flag")
        interest(2)
    }

    field(CO26CHGD, DBF_UCHAR) {
        prompt("Optional column 26 changed flag")
        interest(2)
    }

    field(CO27CHGD, DBF_UCHAR) {
        prompt("Optional column 27 changed flag")
        interest(2)
    }

    field(CO28CHGD, DBF_UCHAR) {
        prompt("Optional column 28 changed flag")
        interest(2)
    }

    field(CO29CHGD, DBF_UCHAR) {
        prompt("Optional column 29 changed flag")
        interest(2)
    }

    field(CO2ACHGD, DBF_UCHAR) {
        prompt("Optional column 2A changed flag")
        interest(2)
    }

    field(CO2BCHGD, DBF_UCHAR) {
        prompt("Optional column 2B changed flag")
        interest(2)
    }

    field(CO2CCHGD, DBF_UCHAR) {
        prompt("Optional column 2C changed flag")
        interest(2)
    }

    field(CO2DCHGD, DBF_UCHAR) {
        prompt("Optional column 2D changed flag")
        interest(2)
    }

    field(CO2ECHGD, DBF_UCHAR) {
        prompt("Optional column 2E changed flag")
        interest(2)
    }

    field(CO2FCHGD, DBF_UCHAR) {
        prompt("Optional column 2F changed flag")
        interest(2)
    }

    field(CO30CHGD, DBF_UCHAR) {
        prompt("Optional column 30 changed flag")
        interest(2)
    }

    field(CO31CHGD, DBF_UCHAR) {
        prompt("Optional column 31 changed flag")
        interest(2)
    }

    field(CO32CHGD, DBF_UCHAR) {
        prompt("Optional column 32 changed flag")
        interest(2)
    }

    field(CO33CHGD, DBF_UCHAR) {
        prompt("Optional column 33 changed flag")
        interest(2)
    }

    field(CO34CHGD, DBF_UCHAR) {
        prompt("Optional column 34 changed flag")
        interest(2)
    }

    field(CO35CHGD, DBF_UCHAR) {
        prompt("Optional column 35 changed flag")
        interest(2)
    }

    field(CO36CHGD, DBF_UCHAR) {
        prompt("Optional column 36 changed flag")
        interest(2)
    }

    field(CO37CHGD, DBF_UCHAR) {
        prompt("Optional column 37 changed flag")
        interest(2)
    }

    field(CO38CHGD, DBF_UCHAR) {
        prompt("Optional column 38 changed flag")
        interest(2)
    }

    field(CO39CHGD, DBF_UCHAR) {
        prompt("Optional column 39 changed flag")
        interest(2)
    }

    field(CO3ACHGD, DBF_UCHAR) {
        prompt("Optional column 3A changed flag")
        interest(2)
    }

    field(CO3BCHGD, DBF_UCHAR) {
        prompt("Optional column 3B changed flag")
        interest(2)
    }

    field(CO3CCHGD, DBF_UCHAR) {
        prompt("Optional column 3C changed flag")
        interest(2)
    }

    field(CO3DCHGD, DBF_UCHAR) {
        prompt("Optional column 3D changed flag")
        interest(2)
    }

    field(CO3ECHGD, DBF_UCHAR) {
        prompt("Optional column 3E changed flag")
        interest(2)
    }

    field(CO3FCHGD, DBF_UCHAR) {
        prompt("Optional column 3F changed flag")
        interest(2)
    }

    # ----------------------------------------------------------------
    # Optional column valid row counts - CO00NROWS .. CO3FNROWS
    # ----------------------------------------------------------------

    field(CO00NROWS, DBF_ULONG) {
        prompt("Optional column 00 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO01NROWS, DBF_ULONG) {
        prompt("Optional column 01 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO02NROWS, DBF_ULONG) {
        prompt("Optional column 02 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO03NROWS, DBF_ULONG) {
        prompt("Optional column 03 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO04NROWS, DBF_ULONG) {
        prompt("Optional column 04 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO05NROWS, DBF_ULONG) {
        prompt("Optional column 05 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO06NROWS, DBF_ULONG) {
        prompt("Optional column 06 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO07NROWS, DBF_ULONG) {
        prompt("Optional column 07 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO08NROWS, DBF_ULONG) {
        prompt("Optional column 08 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO09NROWS, DBF_ULONG) {
        prompt("Optional column 09 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO0ANROWS, DBF_ULONG) {
        prompt("Optional column 0A number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO0BNROWS, DBF_ULONG) {
        prompt("Optional column 0B number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO0CNROWS, DBF_ULONG) {
        prompt("Optional column 0C number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO0DNROWS, DBF_ULONG) {
        prompt("Optional column 0D number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO0ENROWS, DBF_ULONG) {
        prompt("Optional column 0E number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO0FNROWS, DBF_ULONG) {
        prompt("Optional column 0F number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO10NROWS, DBF_ULONG) {
        prompt("Optional column 10 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO11NROWS, DBF_ULONG) {
        prompt("Optional column 11 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO12NROWS, DBF_ULONG) {
        prompt("Optional column 12 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO13NROWS, DBF_ULONG) {
        prompt("Optional column 13 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO14NROWS, DBF_ULONG) {
        prompt("Optional column 14 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO15NROWS, DBF_ULONG) {
        prompt("Optional column 15 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO16NROWS, DBF_ULONG) {
        prompt("Optional column 16 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO17NROWS, DBF_ULONG) {
        prompt("Optional column 17 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO18NROWS, DBF_ULONG) {
        prompt("Optional column 18 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO19NROWS, DBF_ULONG) {
        prompt("Optional column 19 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO1ANROWS, DBF_ULONG) {
        prompt("Optional column 1A number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO1BNROWS, DBF_ULONG) {
        prompt("Optional column 1B number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO1CNROWS, DBF_ULONG) {
        prompt("Optional column 1C number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO1DNROWS, DBF_ULONG) {
        prompt("Optional column 1D number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO1ENROWS, DBF_ULONG) {
        prompt("Optional column 1E number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO1FNROWS, DBF_ULONG) {
        prompt("Optional column 1F number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO20NROWS, DBF_ULONG) {
        prompt("Optional column 20 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO21NROWS, DBF_ULONG) {
        prompt("Optional column 21 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO22NROWS, DBF_ULONG) {
        prompt("Optional column 22 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO23NROWS, DBF_ULONG) {
        prompt("Optional column 23 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO24NROWS, DBF_ULONG) {
        prompt("Optional column 24 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO25NROWS, DBF_ULONG) {
        prompt("Optional column 25 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO26NROWS, DBF_ULONG) {
        prompt("Optional column 26 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO27NROWS, DBF_ULONG) {
        prompt("Optional column 27 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO28NROWS, DBF_ULONG) {
        prompt("Optional column 28 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO29NROWS, DBF_ULONG) {
        prompt("Optional column 29 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO2ANROWS, DBF_ULONG) {
        prompt("Optional column 2A number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO2BNROWS, DBF_ULONG) {
        prompt("Optional column 2B number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO2CNROWS, DBF_ULONG) {
        prompt("Optional column 2C number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO2DNROWS, DBF_ULONG) {
        prompt("Optional column 2D number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO2ENROWS, DBF_ULONG) {
        prompt("Optional column 2E number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO2FNROWS, DBF_ULONG) {
        prompt("Optional column 2F number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO30NROWS, DBF_ULONG) {
        prompt("Optional column 30 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO31NROWS, DBF_ULONG) {
        prompt("Optional column 31 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO32NROWS, DBF_ULONG) {
        prompt("Optional column 32 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO33NROWS, DBF_ULONG) {
        prompt("Optional column 33 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO34NROWS, DBF_ULONG) {
        prompt("Optional column 34 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO35NROWS, DBF_ULONG) {
        prompt("Optional column 35 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO36NROWS, DBF_ULONG) {
        prompt("Optional column 36 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO37NROWS, DBF_ULONG) {
        prompt("Optional column 37 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO38NROWS, DBF_ULONG) {
        prompt("Optional column 38 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO39NROWS, DBF_ULONG) {
        prompt("Optional column 39 number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO3ANROWS, DBF_ULONG) {
        prompt("Optional column 3A number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO3BNROWS, DBF_ULONG) {
        prompt("Optional column 3B number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO3CNROWS, DBF_ULONG) {
        prompt("Optional column 3C number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO3DNROWS, DBF_ULONG) {
        prompt("Optional column 3D number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO3ENROWS, DBF_ULONG) {
        prompt("Optional column 3E number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }

    field(CO3FNROWS, DBF_ULONG) {
        prompt("Optional column 3F number of valid rows")
        special(SPC_NOMOD)
        interest(2)
    }
}
